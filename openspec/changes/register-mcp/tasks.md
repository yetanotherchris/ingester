# Tasks: Add `zolam mcp` command

- [x] Add `newMcpCmd()` function in `src/cmd/zolam/main.go` with provider argument
- [x] Register the command in `rootCmd.AddCommand()`
- [x] Update README Claude Code Integration section to reference `zolam mcp claude`
- [x] Verify with review agents
