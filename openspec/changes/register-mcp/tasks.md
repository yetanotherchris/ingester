# Tasks: Add `zolam mcp` command

- [ ] Add `newMcpCmd()` function in `src/cmd/zolam/main.go`
- [ ] Register the command in `rootCmd.AddCommand()`
- [ ] Update README Claude Code Integration section to reference `zolam mcp`
- [ ] Verify with review agents
